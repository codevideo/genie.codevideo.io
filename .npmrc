; codevideo-ide-react peers react@^18 while this app runs react@19; that pairing
; is intentional and works, so install with legacy-peer-deps everywhere (local +
; Netlify CI), matching how it's installed by hand.
legacy-peer-deps=true
